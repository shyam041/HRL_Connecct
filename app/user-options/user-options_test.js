'use strict';

describe('myApp.user-options module', function() {

  beforeEach(module('myApp.user-options'));

  describe('UserOptionsCtrl controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view2Ctrl = $controller('UserOptionsCtrl');
      expect(view2Ctrl).toBeDefined();
    }));

  });
});
