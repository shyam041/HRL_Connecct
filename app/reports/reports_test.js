'use strict';

describe('myApp.reports module', function() {

  beforeEach(module('myApp.reports'));

  describe('ReportsCtrl controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view2Ctrl = $controller('ReportsCtrl');
      expect(view2Ctrl).toBeDefined();
    }));

  });
});
